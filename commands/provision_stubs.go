package commands

// Stubs — replaced task by task. Remove this file when all resource files exist.

func applyOrgMembers(c *provisionClient, dir string, orgID uint) error        { return nil }
func applyProviders(c *provisionClient, dir string, orgID uint) (uint, error)  { return 0, nil }
func applyPortal(c *provisionClient, dir string, orgID uint) error             { return nil }
func applyKPI(c *provisionClient, dir string, orgID uint) error                { return nil }
func applyClientGroups(c *provisionClient, dir string, orgID uint, prune bool) error { return nil }
func applyNav(c *provisionClient, dir string, orgID uint) error                { return nil }
func applyDashboards(c *provisionClient, dir string, orgID, providerID uint, draft bool, previewDataset string) (int, error) {
	return 0, nil
}
func applySites(c *provisionClient, dir string, orgID uint) error          { return nil }
func applyCredentials(c *provisionClient, dir string, orgID uint) error    { return nil }
func applyImageTaxonomy(c *provisionClient, dir string, orgID uint) error  { return nil }
func applyPlaybooks(c *provisionClient, dir string, orgID uint) error      { return nil }
func applyTestSuites(c *provisionClient, dir string, orgID uint) error     { return nil }
func applyRouterRules(c *provisionClient, dir string, orgID uint) error    { return nil }
func applyWidgets(c *provisionClient, dir string, orgID uint) error        { return nil }
func applyKnowledge(c *provisionClient, dir string, orgID uint) error      { return nil }
func applyImagesManifest(c *provisionClient, dir string, orgID uint) error { return nil }
