package commands

// Stubs — replaced task by task. Remove this file when all resource files exist.

// provisionCredentialListItem is a type alias for credentialListItem (defined in
// provision_credentials.go). Playbooks reference it by this name.
type provisionCredentialListItem = credentialListItem
